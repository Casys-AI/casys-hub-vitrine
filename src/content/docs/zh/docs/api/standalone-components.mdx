---
title: 独立组件
description: 独立使用 RateLimiter、RequestQueue 和 SchemaValidator。
next: false
---

import { Aside } from '@astrojs/starlight/components';

`@casys/mcp-server` 的构建模块可以独立使用。速率限制、并发控制和 Schema 验证 — 可在任何 Deno 或 Node.js 项目中使用，无需 MCP。

## RateLimiter

滑动窗口速率限制器，支持按键追踪。无外部依赖。

```typescript
import { RateLimiter } from "@casys/mcp-server";

const limiter = new RateLimiter({
  maxRequests: 10,   // 10 requests
  windowMs: 1000,    // per 1 second
});
```

### checkLimit

```typescript
limiter.checkLimit(key: string): boolean;
```

在限制内返回 `true`，被限流时返回 `false`。非阻塞 — 不会等待。

```typescript
if (limiter.checkLimit("client-123")) {
  // Process request
} else {
  // Rate limited
}
```

### waitForSlot

```typescript
await limiter.waitForSlot(key: string): Promise<void>;
```

阻塞直到有可用槽位。最大等待时间等于 `windowMs`。

```typescript
await limiter.waitForSlot("client-123");
// Guaranteed to have a slot when this resolves
```

### getRemainingRequests

```typescript
limiter.getRemainingRequests(key: string): number;
```

返回给定键在当前窗口内还可以发出的请求数。对于没有历史记录的键返回 `maxRequests`。

```typescript
const remaining = limiter.getRemainingRequests("client-123");
// e.g., 7 if 3 of 10 requests have been used
```

### getTimeUntilSlot

```typescript
limiter.getTimeUntilSlot(key: string): number;
```

返回给定键下一个槽位开放前的毫秒数。如果槽位已可用，返回 `0`。

### clear

```typescript
limiter.clear(key: string): void;
```

清除特定键的请求历史，立即释放其槽位。

### clearAll

```typescript
limiter.clearAll(): void;
```

清除所有追踪的键及其请求历史。

### getMetrics

```typescript
limiter.getMetrics(): { keys: number; totalRequests: number };
```

## RequestQueue

可配置背压的并发控制。保护下游服务免受过载。

```typescript
import { RequestQueue } from "@casys/mcp-server";

const queue = new RequestQueue({
  maxConcurrent: 5,
  strategy: "queue",     // "sleep" | "queue" | "reject"
  sleepMs: 10,           // For "sleep" strategy only — default: 10
});
```

### 策略

| 策略 | 行为 | 适用场景 |
|------|------|---------|
| `sleep` | 以 `sleepMs` 间隔自旋等待直到槽位释放 | 低延迟，可容忍 CPU |
| `queue` | FIFO 队列 — 请求按顺序等待 | 公平调度，可预测延迟 |
| `reject` | 满载时立即拒绝 | 快速失败，断路器风格 |

### acquire / release

```typescript
await queue.acquire(): Promise<void>;
queue.release(): void;
```

始终使用 try/finally 防止槽位泄漏：

```typescript
await queue.acquire();
try {
  await processRequest();  // 1 of maxConcurrent slots occupied
} finally {
  queue.release();
}
```

### getMetrics

```typescript
queue.getMetrics(): { inFlight: number; queued: number };
```

## SchemaValidator

基于 ajv 8 的 JSON Schema 验证，带 `allErrors` 和 `useDefaults`。Schema 在 `addSchema()` 时一次性编译 — 验证速度快。

```typescript
import { SchemaValidator } from "@casys/mcp-server";

const validator = new SchemaValidator();
```

### addSchema

```typescript
validator.addSchema(name: string, schema: object): void;
```

编译并缓存 schema。在启动时调用一次：

```typescript
validator.addSchema("my_tool", {
  type: "object",
  properties: {
    count: { type: "number", minimum: 0 },
    label: { type: "string", maxLength: 100 },
  },
  required: ["count"],
});
```

### validate

```typescript
validator.validate(name: string, data: unknown): ValidationResult;
```

```typescript
interface ValidationResult {
  valid: boolean;
  errors: ValidationError[];  // Structured error objects (empty if valid)
}
```

```typescript
validator.validate("my_tool", { count: -1 });
// { valid: false, errors: ["count must be >= 0"] }

validator.validate("my_tool", { count: 5 });
// { valid: true, errors: [] }
```

### hasSchema

```typescript
validator.hasSchema(name: string): boolean;
```

如果已添加给定名称的 schema，返回 `true`。

### removeSchema

```typescript
validator.removeSchema(name: string): void;
```

移除之前添加的 schema。之后以该名称调用 `validate()` 将抛出错误。

### validateOrThrow

```typescript
validator.validateOrThrow(name: string, data: unknown): void;
```

数据无效时抛出 `ValidationError`。在希望快速失败而非处理结果对象时很有用：

```typescript
try {
  validator.validateOrThrow("my_tool", args);
  // args are valid — proceed
} catch (error) {
  // error.message contains all validation errors
}
```

<Aside type="tip">
  在 `McpApp` 内部，这些组件根据你的选项自动装配。独立使用仅在框架之外构建自定义基础设施时需要。
</Aside>

## 另请参阅

- [McpApp API](/zh/docs/api/concurrent-mcp-server/) — 这些组件在内部的使用方式
- [中间件管道](/zh/docs/guides/middleware/) — 管道中的速率限制、验证和背压
